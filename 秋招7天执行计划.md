# UpgradeLab 秋招版：7 天执行计划

日期：2026-09-18。此计划收缩此前完整设计，以完成可运行、可解释、可复现的第一版为目标。项目尚未实现，下面是任务安排，不是完成记录。

## 第一个发布版本

一句话：读取 Python 依赖升级后的失败测试，定位相关代码，生成并实际验证补丁，输出 diff 与证据报告。

开发阶段使用自己可控的一个 Python 仓库和一种固定版本迁移。技术栈先使用 Python、OpenRath、pytest、Git 和容器执行器；命令行作为第一入口。选定 OpenRath 版本后，验证 API 再编写适配器。持久化先采用官方示例已展示的 SQLite 路径。

第一版必做：失败复现、按失败证据选取代码、单候选补丁、独立验证、diff 和结构化报告、固定任务输入、同模型基线比较。恢复能力在最小闭环跑通后加入。双候选、多个依赖生态、React 工作台、公开任意仓库执行和远程 PR 发布留到后续。

## 今天：用半天固定设计

设计成果只需要四项：

1. 一个案例：升级前通过、升级后失败、人工参考补丁可以修复。记录仓库和版本来源，能执行复现命令。
2. 一个验收定义：目标依赖保持不变，原有可见测试和独立业务契约测试通过，没有修改测试、吞异常或降级。
3. 一条工作流：复现 → 定位 → 生成补丁 → 应用 → 验证 → 报告。验证失败最多返回修复两次，总生成轮数最多三次。
4. 一个模块边界：workflow 负责流程，context 负责代码选择，executor 负责受控执行，verifier 负责验收，report 负责证据，runtime_adapter 隔离上游接口。

输入字段：task_id、repo_path、base_commit、target_dependency、target_version、test_command、max_rounds、token_budget。

输出字段：status（PASS / FAIL / ERROR）、patch_path、selected_files、verification_results、model_id、token_usage、elapsed_seconds、failure_reason。PASS 必须有实际执行证据。

半天后的判断：是否能在本地证明这个案例确实可修复？若不能，先解决样例和环境，不扩大架构。

## 按交付物推进

| 日期 | 开发工作 | 当天必须能展示什么 |
| --- | --- | --- |
| 第 1 天 | 固定模型、SDK、样例及环境；跑通最小工具调用；人工复现升级失败 | 基线通过、升级失败、参考补丁修复成功三份实际结果 |
| 第 2 天 | 单候选 Agent 流程；受控读文件、应用补丁与运行测试 | 一个真实模型生成并验证的 patch.diff，失败也记录 |
| 第 3 天 | 失败栈解析和 import 关系选择；保存选取原因 | 自己实现的上下文选择模块和可解释的文件清单 |
| 第 4 天 | 冻结补丁后独立验收；整理 5—8 个不同根因任务 | 能拒绝降级、改测试等错误方案；逐任务结果报告 |
| 第 5 天 | 对照单 Agent 和自己的选择策略，使用同模型、工具、预算上限；每任务尽量重复 3 次 | 原始运行记录、成功率、成本、失败案例，不预设提升幅度 |
| 第 6 天 | 加一个真实进程退出恢复案例；保存并核对代码与测试证据的关联 | 中断后恢复；旧代码上的测试结果不能用于新补丁 |
| 第 7 天 | README、干净环境复现、短视频、静态结果展示；总结贡献与限制 | 可分享的仓库材料、90 秒演示、带真实 diff 的报告 |

这是按已有 Python 基础、每天能持续投入安排的目标；环境和 SDK 验证可能改变节奏。第五天若闭环仍未完成，先集中完成修复和验证，把恢复及网页交互顺延。

第 2 天只需跑通一个案例，不宣称通用修复能力。第 4 天的任务可先用自建固定样例，后续增加允许复用的公开历史案例并明确标注来源。

## 最小架构

```text
CLI
 └─ OpenRath workflow（通过 runtime_adapter 调用）
     ├─ executor：复现失败、受控工作区和命令执行
     ├─ context：失败栈 + import 关系 → 有界文件集合
     ├─ repair：模型生成结构化补丁
     ├─ verifier：目标版本 + 可见测试 + 独立契约验证
     └─ report：diff + 实际测试结果 + 调用成本
```

每个任务有独立工作目录，保留干净 base commit。测试和代码执行进入容器，第一版只接自有样例。Agent 不接触独立验收测试；冻结最终补丁后，在独立验证环境执行，验收失败不反馈给同轮 Agent 调参。

## 秋招交付检查

- 从干净环境按 README 能复现一个完整任务。
- 有真实模型调用，不能把固定参考补丁伪装成 Agent 输出。
- 每条 PASS 对应当前代码和指定依赖环境的测试结果。
- 有一个自己实现、能解释设计取舍的模块。
- 有基线对照和失败案例；数据直接来自运行记录。
- 项目说明明确 OpenRath 已有能力与自己的新增实现。
- 简历只写已完成的部分；未完成模块在路线图标为计划。

展示页面优先使用生成的静态 HTML 报告或 Markdown + diff 下载；真实在线任务服务后续单独部署。当前计划不发布远程仓库、网页或 PR。

## 项目与求职同时推进

现在就开始投递适合的岗位，不等待完整平台完成。项目达到修复闭环时可作为在研项目介绍；进入面试前准备：一段真实运行演示、一个失败案例、一个对照结果、上游和个人贡献边界。每天给投递、笔试和基础知识保留固定时间。

## 上游参考

[OpenRath 官方场景示例](https://github.com/Rath-Team/OpenRath-Example)已展示固定输入、SQLite 状态、检查点、验收和制品输出，可参考持久化接入方式。自己的领域修复逻辑单独实现。
